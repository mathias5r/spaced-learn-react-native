import React from 'react';
import styled from 'styled-components';

const Container = styled.View`
  flex: 1;
  background-color: blue;
`;

const MenuComponent = () => <Container />;

export default MenuComponent;
